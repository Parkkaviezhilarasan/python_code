class Solution(object):
    def missingNumber(self, nums):
        xor_all=0
        xor_arr=0

        n=len(nums)

        for i in range(1,n+1):
            xor_all^=i
        for num in nums:
            xor_arr ^=num
        return xor_all^xor_arr
