class Solution(object):
    def findMaxConsecutiveOnes(self, nums):
        i=0
        maxcount=0
        for j in nums:
            if j==0:
                i=0
            else:
                i+=1
                maxcount=max(maxcount,i)
        return maxcount
