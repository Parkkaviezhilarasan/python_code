class Solution(object):
    def moveZeroes(self, nums):
        non_zero=[]
        zero_count=0
        for num in nums:
            if num != 0:
                non_zero.append(num) 
            else:
                zero_count  += 1
        nums[:] = non_zero + [0] * zero_count
        return nums
