class Solution(object):
    def findMaxConsecutiveOnes(self, nums):
        max_count=0
        for key, group in groupby(nums):
            if key==1:
                max_count = max(max_count, len(list(group)))
        return max_count
